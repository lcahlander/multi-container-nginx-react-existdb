upstream backend {
  server backend:8080;
}

upstream client {
  server client:3000;
}

server {
  listen 80;

  location /exist {
    rewrite /exist/(.*) /$1 break;
    proxy_pass http://backend/exist;
  }

  location / {
    proxy_pass http://client;
  }
}
